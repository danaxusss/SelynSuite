import { Module } from '@nestjs/common';

// Identity bounded context: tenants, users, memberships, sessions, RBAC, audit log.
// Implementation begins in Phase 1.
@Module({})
export class IdentityModule {}

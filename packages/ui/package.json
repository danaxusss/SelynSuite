{
  "name": "@selyn/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared React components for the Selyn Suite. Empty until needed.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"no source yet\" && exit 0",
    "typecheck": "echo \"no source yet\" && exit 0",
    "test": "echo \"no source yet\" && exit 0",
    "build": "echo \"no source yet\" && exit 0"
  }
}
